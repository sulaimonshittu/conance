run:
  timeout: 5m
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - typecheck
    - gosec
    - gofmt
    - goimports
  disable-all: true
issues:
  exclude-use-default: false
